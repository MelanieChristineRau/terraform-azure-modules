# Examples

Working copies you can run.

| Example | What it shows |
|---|---|
| [resource-group/examples/basic](../building-blocks/foundation/resource-group/examples/basic) | Smallest apply: one named resource group |
| [patterns/ai-hackathon](../patterns/ai-hackathon) | Full environment plus budget |

Copy `terraform.tfvars.example` to `terraform.tfvars` in that folder. Never commit `terraform.tfvars`.
